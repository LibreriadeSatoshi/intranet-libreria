# Decision Log — lds-intranet PRD

Canonical memory and audit trail. Every decision, change, and override is recorded here.

## 2026-06-16

- **Intent:** Create (no existing PRD; `_bmad-output/planning-artifacts` empty). Confirmed via `bmad-help` → user selected PRD.
- **Setup:** Customization resolver failed (Python < 3.11); used `customize.toml` defaults directly. No team/personal overrides found. No `project-context.md`. No prior in-progress PRD runs.
- **Existing inputs identified:** `docs/` folder — `overview.md`, `roadmap.md`, `risks.md`, `README.md`, `stories/` (10 per-story specs, status `draft`), `platform/` (PA/PB/PC/P101 specs). Epic ENG-271.

### Brain-dump context (from user, verbal)
- **Driver / why now:** Need to onboard ~**50 new courses**; must make it easy for instructors to create courses in one smooth procedure, capturing all info the Operations team needs **outside of Moodle**.
- **Audience for PRD:** Ifuensan + dev team (no external stakeholder sign-off).
- **Vision beyond v1:** Build an intranet with **public and private fronts**. v1 = instructor onboarding + course creation. Future features intended but **not yet conceived** — PRD should not over-scope them.
- **No additional inputs** beyond `docs/`.
- **New integration fact:** They use **customer.io** for transactional email, lead registration, etc., and will integrate it with the intranet. (Note: this differs from `docs/` story 06b/07 which reference Metricool/Canva for marketing; customer.io is the email/lead layer — reconcile during drafting.)

### Working mode + discovery answers (2026-06-16)
- **Working mode:** Fast path (batch gaps → draft with `[ASSUMPTION]` tags → iterate).
- **Q1 Identity scope — DECISION: full 3-method federation in v1** (local username/password + Google OIDC + GitHub OIDC via self-hosted Authentik). User overrode the docs' GitHub-only-pilot recommendation. **Rationale:** 2 developers dedicated to the identity work, so the infra cost is affordable in the 3-month window. Story 00 is a full v1 capability, not a thin MVP.
- **Q2 customer.io — DECISION (default):** v1 uses customer.io for (a) transactional/notification emails (profile submitted, course approved, course published + promo kit) and (b) lead capture when a user hits "Teach on LdS" (register lead even before wizard completion).
- **Q3 Public vs private front — DECISION (default):** v1 ships the private front only (wizard, drafts, approval, handoff, dashboards). Public instructor profiles + course landing pages (story 08) deferred to phase 2.
- **Q4 Roles — DECISION (default):** four intranet roles exist (instructor, reviewer/Ops, marketing, admin); v1 exercises instructor + reviewer/Ops primarily.
- **Q5 Success metrics — DECISION:** courses onboarded end-to-end (target tied to 50), instructor completion rate (started → submitted), time from "Teach on LdS" → submitted, Ops time-to-approval.
- **Q6 Timeline — DECISION:** ~3 months to deliver, driving the 50-course goal.

### Extraction complete
- Three subagents extracted `docs/` (overview/roadmap/risks, 10 stories, platform track). Digests held by parent. Key findings: MVP cut = stories 00(thin GitHub-OIDC)/01/02/03/05/06 + platform PA/PC/P101 minimal; platform track (PA/PB/PC/P101) all `undefined` and on the critical path; major open decisions = identity scope, post-publish data consistency, dual-handoff-mechanism conflict, artifact-generation-pipeline ambiguity.

### Open Questions triage (2026-06-16)
- **OQ-1 (handoff mechanism) — RESOLVED.** Merge dispara el build automáticamente; la intranet registra el evento + avisa a Operaciones; loader construye en Moodle oculto; el instructor termina el montaje en Moodle; validación final en Moodle antes de abrir a estudiantes. La intranet termina su responsabilidad en `published` (build + Moodle link, oculto). FR-33/34/37a.
- **OQ-2 (post-publish consistency) — RESOLVED.** Fire-and-forget desde la intranet. Lifecycle de contenido: intranet (wizard/draft) → Markdown en GitHub (instructor edita ahí tras enviar; intranet read-only) → Moodle tras publicar (edits solo en Moodle). FR-17a/36a.
- **OQ-7 (confirmation email) — RESOLVED.** Requerido en el wizard. FR-10.
- **OQ-3/4/5/6 — DEFERRED** a arquitectura (dueño: dev team / bmad-create-architecture). OQ-6 (spike Moodle / platform track) es la prioridad siguiente; OQ-3 antes de construir §4.6; OQ-4 antes de integrar §4.1+§4.5; OQ-5 baja prioridad.

### Finalize — input reconciliation (2026-06-16)
Subagent compared docs/ (stories + overview/roadmap/risks) vs PRD. Fixes applied:
- Added Moodle-inversion before/after diagram + rationale to §1 (qualitative intent recovered); noted draft = intranet-owned entity, no longer hidden Moodle course.
- FR-1: annotated full-federation-in-v1 as a deliberate decision (not GitHub-only pilot).
- FR-2a (new): existing-student→teacher friction case must have a defined linking strategy + fallback.
- FR-30a (new): built course placed in the Moodle category assigned by the reviewer.
Not changed (resolved on purpose): confirmation email optional→required (user decision); multi-draft deferral (correct); marketing tooling naming (addendum clarifies).

### Finalize — quality review (2026-06-16)
Reviewer subagent (rubric) → review-quality.md. Fixes applied:
- C-2 status lifecycle: added canonical status table + PR-state mapping to §4.5; FR-26 now maps to it.
- C-3 access control: added role × capability matrix + FR-7a.
- C-1 handoff: aligned addendum §B to the OQ-1 decision (merge triggers build; internal orchestration still for architecture).
- H-2 rejection path: FR-24a (notify w/ reason, read-only, re-attempt = new submission).
- H-3 assets: FR-16a (types/limits/storage/reference flow).
- H-4 analytics: FR-22 promoted to [v1] (capture lifecycle events+timestamps from day one).
- H-5: OQ-4 marked build-blocking (FR-2a/FR-6 v1 but unbuildable until resolved).
- H-1 acceptance criteria: deferred to bmad-create-epics-and-stories by design; noted in §0.
Medium/Low findings left in review-quality.md for the architecture/stories phases.

### Finalize closed (2026-06-16)
- Decision-log audited; input reconciliation + reviewer pass done; open items triaged (3 resolved, OQ-3/4/5/6 deferred with owners).
- Editorial polish: light self-pass (internal-tool scale); structure/prose deemed sound.
- Status set to `final`. Next: bmad-create-architecture (priority OQ-6 spike, then OQ-3/OQ-4).
